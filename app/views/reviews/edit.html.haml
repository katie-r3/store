%h1
  Edit review for:
  = @item.name.capitalize

= simple_form_for([@item, @review]) do |f|
  #rating-form
    = f.label :rating
  = f.label :comment
  = f.input :comment, label: false, :input_html => {:cols => 80, :rows => 10}
  = f.submit
